/**
 * Segmented Control Component
 * 
 * A component that allows users to select one option from a group of options.
 * Similar to radio buttons but with a more visual presentation.
 * 
 * Built using Melt UI's Radio Group component.
 */

import SegmentedControlItem from './segmented-control-item.svelte';
import SegmentedControlText from './segmented-control-text.svelte';
import SegmentedControl from './segmented-control.svelte';

export {
  SegmentedControl,
  SegmentedControlItem,
  SegmentedControlText
};

export default SegmentedControl; 