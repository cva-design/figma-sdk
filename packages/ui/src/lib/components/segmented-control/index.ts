/**
 * Segmented Control Component
 *
 * A component that allows users to select one option from a group of options.
 * Similar to radio buttons but with a more visual presentation.
 *
 * Built using Melt UI's Radio Group component.
 */
//@index(['./[^\.]+.svelte'], (f, {pascalCase}) => `export { default as ${pascalCase(f.path)} } from '${f.path}${f.ext}';`)
export { default as SegmentedControl } from './segmented-control.svelte';
//@endindex
